import { ListToVisualizePipe } from './list-to-visualize.pipe';

describe('ListToVisualizePipe', () => {
  it('create an instance', () => {
    const pipe = new ListToVisualizePipe();
    expect(pipe).toBeTruthy();
  });
});
